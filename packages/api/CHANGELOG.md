# @moenet/api

## 3.0.1

### Patch Changes

- 57c6589: `/health` now reports the running version from `package.json` instead of a hardcoded string, so the version it serves tracks the released version automatically.
